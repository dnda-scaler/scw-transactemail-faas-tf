{
  "name": "email_sender_faas",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist  && mkdir dist && cp package.json dist && cp -R  src/* dist && cp -R node_modules dist/node_modules && cd dist  && zip -qr email_sender_faas.zip * "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
  },
  "dependencies": {
    "got": "11.8.3",
    "nodemailer": "^6.8.0"
  }
}
